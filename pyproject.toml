[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sportage-arbitrage"
version = "0.5.0"
description = "Net sports-arbitrage scanner with unified bookmaker connectors, provider health and execution-aware backtesting."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27,<1",
  "pydantic>=2.7,<3",
  "typer>=0.12,<1",
  "rich>=13.7,<15",
]

[project.optional-dependencies]
dev = ["pytest>=8,<10", "ruff>=0.6,<1"]
ui = ["streamlit>=1.37,<2"]

[project.scripts]
arbengine = "arbengine.cli:app"
sportage = "arbengine.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["src/arbengine"]

[tool.hatch.build.targets.sdist]
include = [
  "/src/arbengine",
  "/config",
  "/tests",
  "/README.md",
  "/pyproject.toml",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 110
